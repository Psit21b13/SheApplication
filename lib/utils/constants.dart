import 'package:flutter/material.dart';

class Constants {
  static const primaryDark = Color(0xff800080);
  static const primaryLight = Color(0xff1a2a6c);

  static const gradientDark = Color(0xff800080);
  static const gradientLight = Color(0xffffc0cb);

  //    color: Colors.grey.shade200,
}
